node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.env.local
.DS_Store
.vscode/*
!.vscode/extensions.json

# Marquee screenshot pipeline: raw PNGs are a build artifact; only the
# optimized public/marquee/*.webp are committed and deployed.
public/marquee/raw/
